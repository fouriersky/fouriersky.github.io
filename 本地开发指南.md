# 本地开发指南

本文档详细说明如何在本地环境中编辑和预览这个 Jekyll 博客网站。

## 关于这个项目

这个网站使用 [Jekyll](https://jekyllrb.com/) 静态站点生成器和 [Chirpy](https://github.com/cotes2020/jekyll-theme-chirpy/) 主题构建。Jekyll 是基于 Ruby 的，这可能与常见的 Node.js 前端项目不同，因此开发环境设置稍有特殊。

## 开发环境要求

### 必需软件

1. **Ruby** (版本 >= 3.0)
2. **RubyGems** (Ruby 的包管理器)
3. **Bundler** (Ruby 依赖管理工具)
4. **Git**

## 方案一：使用 VS Code Dev Container（推荐）

这是最简单的方法，无需在本地安装 Ruby 环境。

### 前置要求
- [Visual Studio Code](https://code.visualstudio.com/)
- [Docker Desktop](https://www.docker.com/products/docker-desktop)
- VS Code 扩展：[Dev Containers](https://marketplace.visualstudio.com/items?itemName=ms-vscode-remote.remote-containers)

### 步骤

1. **克隆仓库**
   ```bash
   git clone https://github.com/fouriersky/fouriersky.github.io.git
   cd fouriersky.github.io
   ```

2. **在 VS Code 中打开**
   ```bash
   code .
   ```

3. **使用 Dev Container**
   - VS Code 会检测到 `.devcontainer` 文件夹
   - 点击右下角弹出的提示 "Reopen in Container"
   - 或者按 `F1`，输入 "Dev Containers: Reopen in Container"

4. **等待容器构建**
   - 首次构建可能需要几分钟
   - 容器会自动安装所有依赖

5. **启动开发服务器**
   ```bash
   bash tools/run.sh
   ```

6. **访问网站**
   - 打开浏览器访问 http://localhost:4000
   - 修改文件后，网站会自动重新生成

## 方案二：本地安装 Ruby 环境

如果不想使用 Docker，可以直接在本地安装 Ruby 环境。

### macOS 系统

1. **安装 Ruby（使用 Homebrew）**
   ```bash
   # 安装 Homebrew（如果还没有）
   /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
   
   # 安装 Ruby
   brew install ruby@3.3
   
   # 添加到 PATH（根据 Homebrew 提示的路径）
   echo 'export PATH="/opt/homebrew/opt/ruby/bin:$PATH"' >> ~/.zshrc
   source ~/.zshrc
   ```

2. **验证安装**
   ```bash
   ruby -v  # 应显示 Ruby 3.x
   gem -v   # 应显示 RubyGems 版本
   ```

3. **克隆仓库并安装依赖**
   ```bash
   git clone https://github.com/fouriersky/fouriersky.github.io.git
   cd fouriersky.github.io
   
   # 安装 Bundler
   gem install bundler
   
   # 安装项目依赖
   bundle install
   ```

4. **启动开发服务器**
   ```bash
   bash tools/run.sh
   # 或直接使用 Jekyll 命令
   bundle exec jekyll serve
   ```

5. **访问网站**
   - 打开浏览器访问 http://127.0.0.1:4000

### Windows 系统

1. **安装 Ruby**
   - 下载并安装 [RubyInstaller](https://rubyinstaller.org/)（推荐 Ruby+Devkit 版本）
   - 安装时选择 "Add Ruby executables to your PATH"
   - 安装完成后运行 `ridk install`，选择 MSYS2 工具链安装

2. **验证安装**
   ```bash
   ruby -v
   gem -v
   ```

3. **克隆仓库并安装依赖**
   ```bash
   git clone https://github.com/fouriersky/fouriersky.github.io.git
   cd fouriersky.github.io
   
   gem install bundler
   bundle install
   ```

4. **启动开发服务器**
   ```bash
   bundle exec jekyll serve
   ```

5. **访问网站**
   - 打开浏览器访问 http://127.0.0.1:4000

### Linux 系统（Ubuntu/Debian）

1. **安装 Ruby 和依赖**
   ```bash
   sudo apt-get update
   sudo apt-get install -y ruby-full build-essential zlib1g-dev
   
   # 配置 gem 安装路径（避免使用 sudo）
   echo '# Install Ruby Gems to ~/gems' >> ~/.bashrc
   echo 'export GEM_HOME="$HOME/gems"' >> ~/.bashrc
   echo 'export PATH="$HOME/gems/bin:$PATH"' >> ~/.bashrc
   source ~/.bashrc
   ```

2. **安装 Bundler**
   ```bash
   gem install bundler
   ```

3. **克隆仓库并安装依赖**
   ```bash
   git clone https://github.com/fouriersky/fouriersky.github.io.git
   cd fouriersky.github.io
   bundle install
   ```

4. **启动开发服务器**
   ```bash
   bash tools/run.sh
   # 或
   bundle exec jekyll serve
   ```

5. **访问网站**
   - 打开浏览器访问 http://127.0.0.1:4000

## 日常使用指南

### 写新文章

1. 在 `_posts` 目录下创建新文件，文件名格式：`YYYY-MM-DD-标题.md`
   ```bash
   # 例如
   touch _posts/2026-01-11-my-new-post.md
   ```

2. 文件开头添加 Front Matter：
   ```yaml
   ---
   title: 文章标题
   date: 2026-01-11 14:00:00 +0800
   categories: [分类1, 分类2]
   tags: [标签1, 标签2]
   ---
   
   文章内容从这里开始...
   ```

3. 保存文件后，开发服务器会自动重新生成网站

### 修改网站配置

- 编辑 `_config.yml` 文件
- 修改配置后需要重启开发服务器才能生效

### 添加图片

1. 将图片放在 `assets/img/` 目录下
2. 在文章中引用：
   ```markdown
   ![图片描述](/assets/img/your-image.jpg)
   ```

### 查看草稿

1. 在 `_drafts` 目录中创建文章（不需要日期前缀）
2. 使用以下命令预览草稿：
   ```bash
   bundle exec jekyll serve --drafts
   ```

## 常用命令

```bash
# 启动开发服务器（带实时重载）
bash tools/run.sh
# 或
bundle exec jekyll serve

# 构建网站（不启动服务器）
bundle exec jekyll build

# 清理构建文件
bundle exec jekyll clean

# 运行测试（检查链接有效性等）
bash tools/test.sh

# 查看 Jekyll 版本
bundle exec jekyll -v

# 更新依赖
bundle update
```

## 常见问题

### Q1: 运行 `bundle install` 时出错

**解决方法：**
```bash
# 确保 Ruby 版本正确
ruby -v  # 应该是 3.0 或更高

# 清理并重新安装
rm -rf vendor/bundle
bundle install
```

### Q2: 启动服务器后无法访问

**解决方法：**
- 检查端口 4000 是否被占用
- 尝试指定其他端口：`bundle exec jekyll serve --port 4001`

### Q3: 修改文件后网站没有更新

**解决方法：**
- `_config.yml` 修改需要重启服务器
- 其他文件应该自动更新，如果没有，尝试 `Ctrl+C` 停止服务器后重启

### Q4: Windows 上安装依赖时出现编译错误

**解决方法：**
- 确保安装了 Ruby+Devkit 版本
- 运行 `ridk install` 安装开发工具链
- 重新运行 `bundle install`

### Q5: 在 Dev Container 中如何访问网站？

**解决方法：**
- VS Code 会自动转发端口
- 查看"端口"标签（PORTS）
- 或直接访问 http://localhost:4000

## 部署说明

这个网站通过 GitHub Actions 自动部署：

1. 推送到 `main` 或 `master` 分支
2. GitHub Actions 自动构建并部署到 GitHub Pages
3. 访问 https://fouriersky.github.io 查看部署的网站

**注意：**本地预览和线上部署可能有细微差异，推送前建议先在本地测试构建：
```bash
JEKYLL_ENV=production bundle exec jekyll build
```

## 项目结构说明

```
.
├── _config.yml          # 网站配置文件
├── _posts/              # 博客文章
├── _drafts/             # 草稿文章
├── _tabs/               # 顶部导航页面
├── _data/               # 数据文件
├── _plugins/            # Jekyll 插件
├── assets/              # 静态资源（图片、CSS、JS）
├── .devcontainer/       # VS Code Dev Container 配置
├── tools/               # 辅助脚本
│   ├── run.sh          # 启动开发服务器
│   └── test.sh         # 运行测试
└── Gemfile              # Ruby 依赖定义
```

## 进一步学习

- [Jekyll 官方文档](https://jekyllrb.com/docs/)
- [Chirpy 主题文档](https://github.com/cotes2020/jekyll-theme-chirpy/wiki)
- [Markdown 语法指南](https://www.markdownguide.org/)
- [Liquid 模板语言](https://shopify.github.io/liquid/)

## 获取帮助

如果遇到问题：
1. 查看 [Jekyll 官方文档](https://jekyllrb.com/docs/troubleshooting/)
2. 查看 [Chirpy 主题 Issues](https://github.com/cotes2020/jekyll-theme-chirpy/issues)
3. 搜索相关错误信息

---

祝您使用愉快！🎉
